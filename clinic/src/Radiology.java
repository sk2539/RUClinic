package clinic.src;

/**
 * The clinic.src.Radiology enum represents different types of radiology services available at the clinic.
 * Each enum constant represents a specific type of imaging service.
 */
public enum Radiology {
    CATSCAN,
    ULTRASOUND,
    XRAY;
}